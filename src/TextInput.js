import './TextInput.css'

export default function TextInput({ processInputFlag, setProcessInputFlag, processInput }) {
  return <input type="text" id="url-input" placeholder="Paste a recipe's URL"/>;
}
